#include <iostream>
#include "src/lib/cpplib.h"

using namespace std;

int main() {
  // Print Hello world!
  CPPLib s; 
  
  // Student_deep d(0);
  // Student_deep e = d;
  // Student_deep f;
  // f = e;
  // cout << *d.id << *e.id << *f.id << endl;
  // *e.id = 2;
  // *f.id = 1;
  // cout << *d.id << *e.id << *f.id << endl; 
  return 0;
}